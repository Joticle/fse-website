# This project follows FlowState Engineering (FSE)

This file exists so AI assistants that auto-load `CLAUDE.md` continue to find methodology context.

The methodology is in `FSE.md`. The session state is in `FSE_STATE.md`. The architecture is in `FSE_DISCOVERY.md`.

Read those before making changes. See https://fstate.dev for the public methodology reference.
